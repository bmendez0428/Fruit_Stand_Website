<%= form_for(@sale) do |f| %>
  <% if @sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% @sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :revenue %><br>
    <%= f.text_field :revenue %>
  </div>
  <div class="field">
    <%= f.label :fruitsold %><br>
    <%= f.number_field :fruitsold %>
  </div>
  <div class="field">
    <%= f.label :fruittypesold %><br>
    <%= f.text_field :fruittypesold %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
